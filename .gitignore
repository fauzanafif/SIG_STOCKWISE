# Reference Excel data (kept locally, not versioned — see docs/excel-data-mapping.md)
*.xlsx
/data-reference/

# Personal Windows Downloads folder — accidentally copied in, not part of the app
/frontend/Downloads/

# Accurate Firebird database — company data, read-only source, never versioned
*.gdb
*.GDB
*.fdb
*.FDB
/database/accurate/
/sync-service/.env
/sync-service/logs/

# Claude Code local settings (personal, not team-shared)
.claude/settings.local.json

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
*.swp

# Dev server / tooling logs
*.log

# Python bytecode cache
__pycache__/
*.pyc

# Root-level tooling
/node_modules/
/vendor/

# Env
.env
.env.*
!.env.example

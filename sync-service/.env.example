# Firebird (Accurate) — read-only source, never write here
FIREBIRD_HOST=localhost
FIREBIRD_PORT=3050
FIREBIRD_DATABASE=D:\STOCKWISE\GUDANGSIG2025.GDB
FIREBIRD_USER=CPS#1
FIREBIRD_PASSWORD=
FIREBIRD_CLIENT_LIB=D:\FirebirdLocal\Firebird_25\bin\fbclient.dll

# MySQL (Stockwise) — local dev default matches Laragon (root / no password)
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_DATABASE=stockwise
MYSQL_USER=root
MYSQL_PASSWORD=

# Stockwise Laravel API (for later phases — sync should eventually POST here
# instead of writing to MySQL directly)
STOCKWISE_API_URL=http://127.0.0.1:8001/api
